<section class="menu-target1">
    <h2>
        <i class="fas fa-box-open"></i> 
        Ressources
    </h2>

    <a class="btn-green btn-lg" 
        href="{{ url('resources!create') }}" 
        title="Cliquer pour créer une nouvelle ressource">
        Nouvelle Ressource
    </a>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Id. Nom
                    <hr>
                    Categorie
                </th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>

            {% for resource in allResources %}
            <tr>
                <td>
                    <a href="https://{{ resource.link }}" 
                        title="https://{{ resource.link }}">
                        {{ resource.id }}. {{ resource.name }}
                        <hr>{{ resource.category }}
                    </a>
                </td>
                <td>
                    <a href="https://{{ resource.link }}" 
                        title="https://{{ resource.link }}">
                        {{ resource.description }}
                    </a>
                </td>
                <td>
                    <a class="btn-blue" 
                        href="{{ url('resources!update', {'id': resource.id}) }}"
                        title="Cliquer pour éditer cette ressource">
                        <i class="fas fa-pen-square fa-fw fa-lg"></i>
                    </a>
                    <a class="btn-red" 
                        href="{{ url('resources!delete', {'id': resource.id}) }}"
                        title="Cliquer pour supprimer cette ressource">
                        <i class="fas fa-times-circle fa-fw fa-lg"></i>
                    </a>
                </td>
            </tr>
            {% endfor %}

        </tbody>
    </table>
</section>
